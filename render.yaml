services:
  - type: web
    name: mapa-ambiental
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: EE_CREDENTIALS
        sync: false
